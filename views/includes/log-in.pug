
block content
  div(class="modal")
    div(id="login__errors",class="errors-container")
    .modal-header
      .button-div
        button(id="close__modal") X
      #header__title Welcome Back.
    form(class="log-in-form")
      #login__email
        label(id="email",for='email') Email
        input(id="email__input",type='email', name='email')
      #login__password
        label(id="password",for='password') Password
        input(id="password__input",type='password', name='password')
      div
        input(type="hidden" name="_csrf" value=csrfToken)
        button(type="submit") Log In
  #overlay
    script(type="module" src="/js/log-in.js")
